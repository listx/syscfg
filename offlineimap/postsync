#!/usr/bin/env bash

# Sync notmuch to offlineimap.
notmuch new

# Organize messages with tags.
notmuch tag --batch --input="$HOME"/syscfg/notmuch/tags

# Move deleted files to Trash folder.
for f in $(notmuch search --output=files tag:deleted folder:INBOX); do
	mv "$f" ~/mail/Trash/cur
done
