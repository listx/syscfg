! default urxvt settings for all urxvt windows, unless overridden
! with commandline arguments
! NOTE: "URxvt" is a class name and applies to all urxvt windows, unless the
! specific setting is overridden; if it's just "urxvt" here instead, then any
! urxvt window with a specified name (e.g., urxvt -name name_of_window) will
! NOT use the "urxvt" settings! That's why we use URxvt here.

! First set base font to Terminus to handle all Latin glyphs
! ...then set Japanese font to take care of most Chinese kanji and also hiragana/katakana
! ...then finally only use Korean font for hangul characters
URxvt*font: xft:Terminus:size=11,xft:Kochi Gothic,xft:Baekmuk Gulim
URxvt*geometry: 100x70

! urxvt colors

! dim the colors by 20 percent when the window is inactive
URxvt*depth: 32
URxvt*fading: 20
URxvt*cursorColor: green

! white-on-black color theme (modified zenburn)
URxvt*background: #22222a
URxvt*foreground: #cccccf
! black (normal, bright; color0 should match background color if white-on-dark)
URxvt*color0:  #22222a
URxvt*color8:  #709080
! white (color7 should match foreground color if white-on-dark)
URxvt*color7:  #cccccf
URxvt*color15: #ffffff
! red
URxvt*color1:  #d78787
URxvt*color9:  #d70000
! green
URxvt*color2:  #60b48a
URxvt*color10: #87ff87
! yellow
URxvt*color3:  #d7d7af
URxvt*color11: #ffd7af
! blue
URxvt*color4:  #94bff3
URxvt*color12: #94bff3
! purple
URxvt*color5:  #dc8cc3
URxvt*color13: #d787af
! cyan
URxvt*color6:  #8cd0d3
URxvt*color14: #afffff

! misc urxvt settings

! bind CTRL+ALT to change to different font sizes
URxvt.keysym.M-C-6: command:\033]710;xft:dejavu sans mono:size=10,xft:Kochi Gothic,xft:Baekmuk Gulim\007
URxvt.keysym.M-C-7: command:\033]710;xft:dejavu sans mono:size=12,xft:Kochi Gothic,xft:Baekmuk Gulim\007
URxvt.keysym.M-C-8: command:\033]710;xft:dejavu sans mono:size=36,xft:Kochi Gothic,xft:Baekmuk Gulim\007
URxvt.keysym.M-C-9: command:\033]710;xft:dejavu sans mono:size=72,xft:Kochi Gothic,xft:Baekmuk Gulim\007
URxvt.keysym.M-C-0: command:\033]710;xft:Terminus:size=10,xft:Kochi Gothic,xft:Baekmuk Gulim\007
URxvt.keysym.M-C-minus: command:\033]710;xft:Terminus:size=11,xft:Kochi Gothic,xft:Baekmuk Gulim\007
URxvt.keysym.M-C-equal: command:\033]710;xft:Terminus:size=16,xft:Kochi Gothic,xft:Baekmuk Gulim\007

! make urxvt the login shell
URxvt*loginShell:   true
!cutchars are characterse used as delimiters for double-click word selection
URxvt*cutchars: "()*,<>[]{}|'
URxvt*saveLines: 32767
URxvt*scrollBar: off
URxvt*urlLauncher: firefox
