#command

# map backspace to back-screen
      back-screen

#env

# -i: make lower-case-only searches case insensitive (mixed-case searches are automatically case-sensitive!)
# -R: make 'git diff' more compliant with highly customized LESS_TERMCAP options
LESS = -iR
